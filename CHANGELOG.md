# 변경 사항 요약 (v2.0)

이번 업데이트에서는 기존의 단일 페이지 애플리케이션 구조를 대대적으로 리팩토링하여, 여러 컴포넌트 기반의 모던한 React 애플리케이션 구조로 개선했습니다. 이를 통해 사용자가 포트폴리오를 저장하고, 불러오고, 수정 및 삭제할 수 있는 완전한 CRUD 기능을 구현했습니다.

## ✨ 주요 변경 사항

### 1. 포트폴리오 관리 기능 추가
- **포트폴리오 목록 보기**: 이제 사용자는 자신이 생성한 모든 포트폴리오를 한눈에 볼 수 있습니다.
- **포트폴리오 불러오기 및 수정**: 목록에서 특정 포트폴리오를 선택하여 기존 내용을 수정하고 다시 저장할 수 있습니다.
- **포트폴리오 삭제**: 더 이상 필요 없는 포트폴리오는 목록에서 간단하게 삭제할 수 있습니다.
- **신규 포트폴리오 생성**: 기존의 포트폴리오 생성 기능은 "새 포트폴리오 만들기" 워크플로우로 통합되었습니다.

### 2. 코드 구조 리팩토링 (컴포넌트 분리)
기존에 모든 기능이 집중되어 있던 `App.js` 파일을 기능별로 여러 컴포넌트로 분리하여 코드의 가독성과 유지보수성을 크게 향상했습니다.

- **`App.js`**: 이제 전체 애플리케이션의 상태(어떤 화면을 보여줄지 등)를 관리하고, 각 컴포넌트 간의 데이터 흐름을 제어하는 최상위 컨트롤러 역할을 합니다.
- **`PortfolioList.jsx` (신규 생성)**: 저장된 포트폴리오 목록을 표시하고, 사용자가 특정 포트폴리오를 선택, 생성 또는 삭제할 수 있는 인터페이스를 제공합니다.
- **`PortfolioBuilder.jsx` (수정)**: 포트폴리오를 '생성'하는 기능과 '수정'하는 기능을 모두 처리하는 범용적인 컴포넌트로 변경되었습니다.
- **`BacktestResults.jsx` (신규 생성)**: 백테스트 실행 결과를 명확하게 보여주는 결과 페이지 컴포넌트입니다.

### 3. API 서비스 개선
- **`client/src/services/api.js`**: 포트폴리오를 수정(`update`)하고 삭제(`delete`)하는 기능을 수행하기 위한 API 요청 함수가 추가되었습니다.

## 📝 파일별 상세 변경 내역

- **`client/src/App.js`**:
  - 기존의 복잡한 단계별 상태 관리 로직을 제거했습니다.
  - `currentView` 상태를 통해 `PortfolioList`, `PortfolioBuilder`, `BacktestResults` 컴포넌트 중 하나를 렌더링하도록 변경했습니다.
  - 서버로부터 포트폴리오 목록을 불러오고, 상태로 관리하는 로직을 추가했습니다.

- **`client/src/components/PortfolioList.jsx`**:
  - 포트폴리오 목록을 보여주는 UI를 담당하는 새로운 컴포넌트입니다.
  - `props`로 포트폴리오 데이터와 `onSelect`, `onDelete`, `onCreateNew` 와 같은 이벤트 핸들러를 전달받습니다.

- **`client/src/components/PortfolioBuilder.jsx`**:
  - 이제 `props`로 `portfolio` 데이터를 받아, 기존 포트폴리오 정보를 폼에 채울 수 있습니다.
  - "저장" 버튼은 상황에 따라 "포트폴리오 저장" 또는 "포트폴리오 수정"으로 텍스트가 변경됩니다.
  - 백테스트 실행, 저장, 취소 등의 동작을 모두 상위 `App.js`의 `props`로 전달된 함수를 호출하여 처리합니다.

- **`client/src/services/api.js`**:
  - `portfolioAPI` 객체에 `update(id, portfolio)`와 `delete(id)` 함수를 추가하여 서버 API와 통신할 수 있도록 했습니다.
  - `backtestAPI`의 `run` 함수가 API 명세에 맞게 단일 인자를 받도록 수정되었습니다.
