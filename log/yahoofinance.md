[Deprecated] historical() relies on an API that Yahoo have removed.  We'll map this request to chart() for convenience, but, please consider using chart() directly instead; for more info see https://github.com/gadicc/yahoo-finance2/issues/795.  This will only be shown once, but you can suppress this message in future with `new YahooFinance({ suppressNotices: ['ripHistorical'] })`.

[더 이상 사용되지 않음] historical() 함수는 Yahoo에서 제거한 API에 의존합니다. 편의상 이 요청을 chart() 함수로 매핑하지만, chart() 함수를 직접 사용하는 것을 권장합니다. 자세한 내용은 https://github.com/gadicc/yahoo-finance2/issues/795를 참조하세요. 이 메시지는 한 번만 표시되며, `new YahooFinance({ suppressNotices: ['ripHistorical'] })`를 사용하여 향후 이 메시지가 표시되지 않도록 설정할 수 있습니다.

[사용 중단됨] historical()은 Yahoo에서 제거한 API에 의존하고 있습니다.
편의를 위해 이 요청을 chart()로 매핑하겠지만, 가능하면 chart()를 직접 사용하는 것을 권장합니다.
자세한 내용은 https://github.com/gadicc/yahoo-finance2/issues/795
 를 참고하세요.
이 메시지는 한 번만 표시되며, 이후에는
new YahooFinance({ suppressNotices: ['ripHistorical'] })
를 사용해 표시를 억제할 수 있습니다